<div class="c-header">
  <h1 class="c-header__title">Status</h1>
</div>

<div class="c-providers">
  {{#each services as |service|}}
    {{component (concat service '-service' )}}
  {{/each}}
</div>

{{outlet}}
