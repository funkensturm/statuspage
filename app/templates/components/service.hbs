{{#if (eq lifecycle 'success')}}

  <a href="#">
    {{#each features as |feature|}}
      {{service-feature serviceName=name feature=feature}}
    {{/each}}
  </a>

{{else}}

  <span class="c-provider__name">
    {{name}}
  </span>

  <span class="c-provider__status">
    {{#if (eq lifecycle 'loading')}}
      Loading...
    {{/if}}

    {{#if (eq lifecycle 'error')}}
      Error!
    {{/if}}
  </span>

{{/if}}
